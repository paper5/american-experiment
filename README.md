<div align="center">

# The American Experiment

**An AI-powered U.S. presidential election simulator that runs entirely in your browser.**

Write the tickets, set the national mood, and let 51 state-level agent swarms deliberate and vote - live, on a real electoral map. **Bring your own API key** - no server, no build step, no sign-up.

[Features](#features) - [Quick start](#quick-start) - [How it works](#how-it-works) - [Testing](#testing) - [Credits](#credits)

</div>

---

**The American Experiment** is a working, self-contained rebuild of the election simulator at [americanexperiment.app](https://www.americanexperiment.app/). It's a single HTML file: the same dark election-night aesthetic, the same 538-electoral-vote map and "the wire" feed - but fully functional with **your own LLM API key**.

> **Note:** Fictional simulation. All candidates, deliberations, and results are generated by language models for entertainment - nothing here is a real prediction.

## Features

- **Ticket generation** - the model writes realistic Democratic and Republican tickets (candidate, running mate, platform, strengths, weaknesses) from just a name - or from a brief you type yourself.
- **State of the Union** - sets the national mood (economy, wars, social tensions) that every state's voters deliberate over.
- **Real election-night simulation** - 51 agent swarms, one per state, vote in parallel. States animate through *Polls open, Researching, Delegates reporting, Called*, shaded by margin.
- **Live "the wire" feed** - every state call streams in as it happens, with a real-time 538-EV scoreboard and a 270-to-win bar.
- **Talk to the voters** - click any state to read its deliberation narrative and ask follow-up questions *in character*.
- **Share the night** - copy a results summary (winner, EVs, biggest margins) to clipboard.
- **Everything persists** - tickets, State of the Union, and settings survive page reloads (`localStorage`).

## Quick start

```bash
# just open the file
open index.html

# or serve it locally
python3 -m http.server 8000
# then visit http://localhost:8000
```

1. Click **Settings** (top-left).
2. Choose your provider, paste your API key, pick a model - hit **Test connection**.
3. Hit **Generate** on each ticket (or type names yourself), write a State of the Union, then **Simulate election**.

That's it. No install, no build, no backend.

## Settings

| Setting | Notes |
|---|---|
| **Provider** | Anthropic (Claude), OpenAI, OpenRouter, or any custom OpenAI-compatible endpoint (Ollama, LM Studio, vLLM, ...) |
| **API key** | Stored only in your browser's `localStorage`; requests go **directly** from your machine to the provider |
| **Model** | Defaults to the provider's flagship; any model id works |
| **Base URL** | For self-hosted gateways |
| **Parallel swarms** | How many states deliberate at once (1-16) - higher is faster, but uses more tokens simultaneously |
| **Temperature** | Agent creativity |

**Test connection** verifies your key and model before you commit to a full simulation.

## How it works

The whole app is one file (`index.html`): the design, the interactive logic, and the embedded SVG geometry for all 51 states (extracted from the original site's saved snapshot).

Every LLM call goes straight from your browser to the API provider using their browser-friendly endpoints:

- **Tickets** - one structured JSON call per ticket (`candidate`, `mate`, `platform`, `strengths`, `weaknesses`).
- **Simulation** - one structured JSON call per state. Each state gets a system prompt containing both tickets, the State of the Union, and its own demographics/political context, and returns `{"winner", "dem_share", "rep_share", "narrative"}`. The swarms run with a configurable concurrency limit, and every state is called independently - so a failure in one state never stops the night.
- **State chat** - a follow-up call per question, seeded with that state's narrative and vote.

An election is **51 requests** - roughly the per-request cost of your provider times 51. With parallel swarms at 5, a full simulation completes in about a minute on typical APIs.

## Project structure

```
american-experiment/
|-- index.html          # the entire app - HTML, CSS, JS, and map data
|-- .test/              # regression suite (no node needed)
|   |-- runtest.py      # assembles + runs the suite under JavaScriptCore
|   |-- harness.js      # minimal DOM + LLM-API stub
|   |-- runner.js       # the assertions
|-- LICENSE             # MIT
```

## Testing

The app's real, unmodified JavaScript is executed under macOS's built-in JavaScriptCore (jsc) against a minimal DOM/API stub, with a fake LLM returning canned JSON. It exercises the whole lifecycle: structure, settings, ticket generation, a full 51-state simulation, winner modal, state chat, and error handling.

```bash
python3 .test/runtest.py
```

```
structure:   OK 51 map paths | OK 42 labels | OK 9 chips | OK 538 EVs
simulation:  OK winner declared | OK 538 EVs | OK all 51 states called ...
ALL TESTS PASSED
```

> The runner uses macOS's `JavaScriptCore` at `/System/Library/Frameworks/.../jsc`. On Linux, point the `JSC` variable at any jsc binary.

## Privacy & cost

- **Your key never touches a server you don't control.** It lives in `localStorage` and is sent only to the provider you chose.
- Everything runs in your browser - there is no analytics, no tracking, no telemetry.
- Watch the **Parallel swarms** setting: it's the dial between speed and token spend.

## Limitations

- The original promises swarms that "research live conditions on X". Without credentials that's not possible here, so agents deliberate from the model's knowledge of current conditions instead.
- Browser requests require the provider to allow CORS. Anthropic, OpenAI, and OpenRouter all do; self-hosted gateways may need a CORS-enabled frontend or proxy.

## Credits

Rebuilt with appreciation for **The American Experiment** ([americanexperiment.app](https://www.americanexperiment.app/)) - original creator [**@vardy.mov**](https://vardy.mov). The map geometry and design language are drawn from the original site's publicly saved snapshot; the implementation, settings, state chat, and testing harness are original to this repository.

## License

[MIT](LICENSE) (c) 2026 SB
